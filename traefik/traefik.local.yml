# Traefik static configuration for local development.
global:
  checkNewVersion: false
  sendAnonymousUsage: false

log:
  level: INFO

accessLog: {}

entryPoints:
  web:
    address: ":80"

providers:
  file:
    filename: /etc/traefik/local/dynamic.yml
    watch: true
